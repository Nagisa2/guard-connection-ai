# App-monitor-v2

医師向けデモ画面と、前段AIを呼び出すFastAPIバックエンドです。起動手順は上位の`README.md`を参照してください。

画面では実測ECGと`PPG由来疑似ECG・診断用ではない`を型とラベルで区別します。低品質・欠損・棄却区間は疑似ECGを補間せず空白表示にします。

`.env.example`を必要に応じて`.env`へコピーしてください。秘密情報や本番認証情報を`.env`へ置いた場合はGitへ追加しないでください。
